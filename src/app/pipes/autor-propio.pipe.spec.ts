import { AutorPropioPipe } from './autor-propio.pipe';

describe('AutorPropioPipe', () => {
  it('create an instance', () => {
    const pipe = new AutorPropioPipe();
    expect(pipe).toBeTruthy();
  });
});
